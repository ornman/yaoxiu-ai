name = "yaoembroidery-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# 环境变量（密钥）
[vars]
ENVIRONMENT = "production"

# 密钥（通过 wrangler secret put 设置，不会暴露在代码中）
# - DEEPSEEK_API_KEY
# - QWEN_API_KEY
